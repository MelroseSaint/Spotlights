import { mutation, query } from "convex/server"; import { v } from "convex/values"; // Follow a user export const followUser = mutation({ args: { followerId: v.id("users"), followingId: v.id("users"), }, handler: async (ctx, { followerId, followingId }) => { // Prevent self-following if (followerId === followingId) { throw new Error("Cannot follow yourself"); } // Check if already following const existingFollow = await ctx.db .query("follows") .filter((q) => q.and( q.eq(q.field("followerId"), followerId), q.eq(q.field("followingId"), followingId) ) ) .first(); if (existingFollow) { throw new Error("Already following this user"); } // Create follow relationship await ctx.db.insert("follows", { followerId, followingId, createdAt: new Date(), }); // Update follower count for the user being followed const followingUser = await ctx.db.get(followingId); if (followingUser) { const followersCount = await ctx.db .query("follows") .filter((q) => q.eq(q.field("followingId"), followingId)) .count(); await ctx.db.patch(followingId, { followers: followersCount, updatedAt: new Date(), }); } // Update following count for the follower const followerUser = await ctx.db.get(followerId); if (followerUser) { const followingCount = await ctx.db .query("follows") .filter((q) => q.eq(q.field("followerId"), followerId)) .count(); await ctx.db.patch(followerId, { following: followingCount, updatedAt: new Date(), }); } return { success: true }; }, }); // Unfollow a user export const unfollowUser = mutation({ args: { followerId: v.id("users"), followingId: v.id("users"), }, handler: async (ctx, { followerId, followingId }) => { // Find and delete the follow relationship const follow = await ctx.db .query("follows") .filter((q) => q.and( q.eq(q.field("followerId"), followerId), q.eq(q.field("followingId"), followingId) ) ) .first(); if (!follow) { throw new Error("Not following this user"); } await ctx.db.delete(follow._id); // Update follower count for the user being unfollowed const followingUser = await ctx.db.get(followingId); if (followingUser) { const followersCount = await ctx.db .query("follows") .filter((q) => q.eq(q.field("followingId"), followingId)) .count(); await ctx.db.patch(followingId, { followers: followersCount, updatedAt: new Date(), }); } // Update following count for the unfollower const followerUser = await ctx.db.get(followerId); if (followerUser) { const followingCount = await ctx.db .query("follows") .filter((q) => q.eq(q.field("followerId"), followerId)) .count(); await ctx.db.patch(followerId, { following: followingCount, updatedAt: new Date(), }); } return { success: true }; }, }); // Get user's followers export const getUserFollowers = query({ args: { userId: v.id("users"), limit: v.optional(v.number()), }, handler: async (ctx, { userId, limit = 10 }) => { const follows = await ctx.db .query("follows") .filter((q) => q.eq(q.field("followingId"), userId)) .order("desc") .take(limit) .collect(); // Get user details for each follower const followers = await Promise.all( follows.map(async (follow) => { const user = await ctx.db.get(follow.followerId); return { ...user, _id: follow.followerId, followedAt: follow._creationTime, }; }) ); return followers; }, }); // Get user's following export const getUserFollowing = query({ args: { userId: v.id("users"), limit: v.optional(v.number()), }, handler: async (ctx, { userId, limit = 10 }) => { const follows = await ctx.db .query("follows") .filter((q) => q.eq(q.field("followerId"), userId)) .order("desc") .take(limit) .collect(); // Get user details for each following const following = await Promise.all( follows.map(async (follow) => { const user = await ctx.db.get(follow.followingId); return { ...user, _id: follow.followingId, followedAt: follow._creationTime, }; }) ); return following; }, }); // Check if current user is following another user export const isFollowing = query({ args: { followerId: v.id("users"), followingId: v.id("users"), }, handler: async (ctx, { followerId, followingId }) => { const follow = await ctx.db .query("follows") .filter((q) => q.and( q.eq(q.field("followerId"), followerId), q.eq(q.field("followingId"), followingId) ) ) .first(); return !!follow; }, });